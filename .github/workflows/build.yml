name: Build & Release (Android + iOS)

on:
  push:
    branches: [main]
  workflow_dispatch:

# release job 需要寫入 Releases 的權限
permissions:
  contents: write

env:
  FLUTTER_VERSION: "3.35.3"

jobs:
  build-android:
    name: Build Android APK
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-java@v4
        with:
          distribution: temurin
          java-version: "17"

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - run: flutter pub get
      - run: flutter build apk --release

      - uses: actions/upload-artifact@v4
        with:
          name: android-apk
          path: frontend/build/app/outputs/flutter-apk/app-release.apk
          if-no-files-found: error

  build-ios:
    name: Build iOS (no codesign)
    runs-on: macos-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v4

      - uses: subosito/flutter-action@v2
        with:
          flutter-version: ${{ env.FLUTTER_VERSION }}
          channel: stable
          cache: true

      - run: flutter pub get
      # CI 無簽署憑證，以 --no-codesign 產出未簽署的 .app
      - run: flutter build ios --release --no-codesign

      - name: Package Runner.app
        run: |
          cd build/ios/iphoneos
          zip -r Runner.app.zip Runner.app

      - uses: actions/upload-artifact@v4
        with:
          name: ios-app
          path: frontend/build/ios/iphoneos/Runner.app.zip
          if-no-files-found: error

  release:
    name: Publish to Releases
    needs: [build-android, build-ios]
    runs-on: ubuntu-latest
    steps:
      - name: Download build artifacts
        uses: actions/download-artifact@v4
        with:
          path: dist

      - name: Stage release assets
        run: |
          mkdir -p assets
          cp dist/android-apk/app-release.apk assets/taiwan-transit.apk
          cp dist/ios-app/Runner.app.zip       assets/taiwan-transit-ios.zip
          ls -lh assets

      - name: Publish rolling "latest" release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: latest
          name: 最新建置 (latest)
          prerelease: true
          # 讓 latest tag 隨每次 build 指向最新 commit
          target_commitish: ${{ github.sha }}
          body: |
            自動建置產物（commit ${{ github.sha }}）
            - `taiwan-transit.apk`：Android（release，debug key 簽署，可側載）
            - `taiwan-transit-ios.zip`：iOS 未簽署 .app（需自行簽署安裝）
          files: assets/*
